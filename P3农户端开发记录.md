# P3 农户端开发完成记录（2026-08-09）

> 目标：农户入驻 + 产品归属 + 认领订单管理 + 收益中心 + 数据看板
> 状态：✅ 全部完成并通过验收（28/28）

---

## 一、后端改动（WisdomTree_Service）

### 实体
- `adminUser.entity.ts`：新增 `farm_name`、`qualification`（资质证号）、`phone`、`status` 字段
- `products.entity.ts` / `product_categories.entity.ts`：新增 `farmer_id`（产品归属农户）

### 新增
- `src/modules/farmer/` 模块（farmer.module/controller/service/dto）
- `src/modules/admin/guard/farmer.guard.ts`：校验 role==='farmer'

### 新增接口（全部 FarmerGuard）
| 接口 | 说明 |
|------|------|
| `POST /admin/farmer/register` | 农户注册（username 唯一校验） |
| `GET /farmer/profile` | 农户信息 |
| `PUT /farmer/profile` | 更新资料 |
| `GET /farmer/products` | 名下产品分类（含认领数/adoptable） |
| `GET /farmer/adoptions` | 认领订单列表（status 筛选） |
| `POST /farmer/adoptions/:id/confirm` | 确认种植（growing→confirmed） |
| `POST /farmer/adoptions/:id/complete` | 履约完成（growing/confirmed→completed） |
| `GET /farmer/revenue` | 收益中心（农户 85% / 平台 15%） |
| `GET /farmer/dashboard` | 数据看板（含 7 天趋势） |

### 登录改造
- `POST /admin/login` 响应 data 增加 `role` 字段（admin/farmer 均可登录）

### 演示账号（密码均 123456）
- `farmer_demo`（绿野生态农场）：分类 1,14,15,16,20,24
- `farmer_demo2`（稻香田园合作社）：分类 12,13,17,18,19,21

## 二、前端改动（WisdomTree_Admin，Vue3+Element Plus）

### 新增页面（src/views/farmer/）
| 页面 | 说明 |
|------|------|
| `FarmerRegister.vue` | 农户注册 |
| `FarmerIndex.vue` | 农户端布局（侧边菜单+顶栏+退出） |
| `FarmerDashboard.vue` | 数据看板（5 统计卡 + ECharts 趋势图） |
| `FarmerAdoptions.vue` | 认领订单（状态筛选 + 确认种植/履约完成） |
| `FarmerProducts.vue` | 产品管理（只读） |
| `FarmerRevenue.vue` | 收益中心（汇总卡 + 明细表） |
| `FarmerProfile.vue` | 个人资料（展示+编辑） |

### 修改
- `LoginForm.vue`：登录后按 role 跳转（farmer→/farmerIndex，admin→原后台）+ 农户注册入口
- `token.js`：加 role 存储
- `router/index.js`：farmer 路由 + 角色守卫

## 三、过程中修复的问题

1. **confirmed 状态筛选被拒**：DTO 的 IsIn 只允许 growing/harvesting/completed，但确认种植后状态是 confirmed → 前端无法筛选/显示 → DTO 加 confirmed + 前端加映射和履约按钮
2. **演示农户 farm_name 乱码**：Agent 用 SQL 插入时双重编码（mojibake）→ 手动 UPDATE 修复为正确 UTF-8

## 四、验收结果（28/28 通过）

farmer 登录（role 带出）→ 农户信息/更新 → 名下产品（6 分类含认领数）→ 认领订单列表/状态筛选 → 确认种植（confirmed）→ 履约完成 → 收益中心（8 单 ¥843.2，平台分成 ¥148.8）→ 数据看板（7 天趋势）→ 权限隔离（farmer 访问 admin 接口 403，farmer_demo2 看不到 demo1 数据）→ 新农户注册/登录/重名拒绝

浏览器实测：农户登录 → 看板（5 卡 + 图表）→ 订单页 → 收益页 全通过

## 五、运行方式

- 后端：`cd WisdomTree_Service && npm run build && node dist/main.js`（8080，tmux wisdom9）
- Admin 前端：`cd WisdomTree_Admin && npm run dev`（5175，tmux admin-p3）
- H5 用户端：`cd WisdomTree_Client && npm run build:h5 && npx vite preview --outDir dist/build/h5`（5173，tmux h5prev4）
- 账号：admin / 密码查 DB；farmer_demo / 123456；testuser / 123456

## 六、下一步（P4 政府端，未开始）

数据驾驶舱 / 商户监管 / 活动统筹（2-3 天）—— 见 `智慧农旅v2总体方案.md` P4 阶段
