# P2 游客体验开发完成记录（2026-08-08）

> 目标：首页重设计（标语+地图+季节）、采摘园地图、收获管理
> 状态：✅ 全部完成并通过验收（17/17）

---

## 一、后端改动（WisdomTree_Service）

### 实体
- `farm_facilities.entity.ts`：新增 `longitude`、`latitude`、`city`（宁夏五市）字段
- `adoptions.entity.ts`：新增 `harvest_type`（pickup/ship）、`harvest_info`（json）字段

### 采摘园数据
- `travel/seed.ts`：设施数据重写为 **12 条宁夏数据**（红颜草莓采摘园/西夏王葡萄庄园/沙坡头西瓜采摘园/中宁枸杞采摘园/吴忠早茶农家乐/六盘山竹林民宿/农耕研学基地等），含真实经纬度；seed 改为按 name **upsert**（幂等）

### 新增接口
| 接口 | 说明 |
|------|------|
| `GET /travel/facilities/map` | 采摘园地图点位（支持 `?type=采摘园` 筛选） |
| `POST /adoption/:id/harvest/pickup` | 预约采摘（growing→harvesting，存预约信息） |
| `POST /adoption/:id/harvest/ship` | 邮寄到家（growing→completed，默认顺丰） |
| `GET /adoption/:id/harvest` | 收获状态查询 |

### 状态流转（幂等校验）
- growing → harvesting（预约采摘）→ completed
- 采摘中再邮寄 → 400「请先完成采摘」；已完成任何操作 → 400「已收获完成」

## 二、前端改动（WisdomTree_Client）

### 新增页面
| 页面 | 说明 |
|------|------|
| `src/pages/farm/FarmMap/FarmMap.vue` | 采摘园地图页 |

### 首页重设计（src/pages/index/index.vue）
- 顶部农旅标语区：「认领宁夏的四季」+ 副标语 + 地图入口 chip
- 季节推荐横滑卡片（春·采摘/夏·亲子/秋·丰收/冬·年货）→ 跳旅程列表
- 采摘园地图入口卡（内嵌宁夏轮廓 SVG + 五市点）→ 跳 FarmMap
- **保留**原有认领农产品/我的认领/智慧旅程/领养/3D 入口

### 公共工具
- `src/common/farm.js`：farmRequest、宁夏投影（线性经纬度→坐标）、五市坐标、设施类型颜色映射、宁夏简化轮廓数据

### 地图实现
- 简易线性投影：lon 105.0-106.6 / lat 35.6-39.2 → 归一化坐标
- SVG 手绘宁夏轮廓 + 5 城市标注 + 12 设施标记（按类型着色：采摘绿/农家乐橙/民宿蓝/研学黄）
- 点击标记弹详情卡（位置/开放时间/电话可拨打/介绍）
- 底部类型筛选 chips（全部/采摘园/农家乐/民宿/研学基地）

## 三、验收结果（17/17 通过）

地图接口 12 条设施含经纬度+字段齐全 → 类型覆盖（采摘园/农家乐/民宿等7类）→ 按类型筛选 → 预约采摘（harvesting）→ 采摘中邮寄被拒(400) → 收获状态查询 → 认领列表含 harvest 字段 → 邮寄到家（completed）→ 邮寄详情含地址/顺丰 → 已完成不能再预约(400)

浏览器实测：首页标语/季节推荐/地图入口 + 地图页 12 标记/5 城市/详情卡/筛选 全通过

## 四、运行方式

- 后端：`cd WisdomTree_Service && npm run build && node dist/main.js`（端口 8080，tmux 会话 wisdom7）
- 前端：`cd WisdomTree_Client && npm run build:h5 && npx vite preview --outDir dist/build/h5`（端口 5173，tmux 会话 h5prev4）
- 测试账号：testuser / 123456

## 五、下一步（P3 农户端，未开始）

农户入驻/上架/接单/履约/收益（3-5 天）—— 见 `智慧农旅v2总体方案.md` P3 阶段
