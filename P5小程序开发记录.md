# P5 小程序开发完成记录（2026-08-09）

> 目标：Client 改造成农旅认领版（TabBar 农旅化 + 小程序构建支持 + 兼容性修复）
> 状态：✅ 全部完成并通过验证（H5 9/9 导航循环 + mp-weixin 编译通过）

---

## 一、改动内容（WisdomTree_Client）

### 1. TabBar 农旅化（src/components/TabBar.vue）
- 用户端导航 5 项改为：**首页 / 认领 / 旅程 / 采摘园 / 我的**
- 移除旧导航（AI助理/环保论坛/环保活动）——页面保留但不在 TabBar
- 图标复用 src/static/images/ 现有 png

### 2. 小程序构建支持
- package.json 新增 `"build:mp-weixin": "uni build --platform mp-weixin"`
- 补依赖 `@dcloudio/uni-mp-weixin` + `@dcloudio/uni-mp-vite`（uni-app 官方标配，缺则退化为 H5 产物）
- manifest.json mp-weixin 配置完善（appid wx66323d9dcc673fee 已有）

### 3. 兼容性修复（条件编译）
- **FarmMap.vue**：SVG 地图 `<!-- #ifdef H5 -->` 隔离；小程序端降级为设施列表模式（复用数据/筛选/详情逻辑）
- **index.vue**：首页地图入口 SVG 条件编译
- **datachart/index.vue**：echarts 条件编译（H5 显示图表，小程序占位）

### 4. 补 TabBar 到农旅页面
- AdoptionList / JourneyList / FarmMap 三个页面补 `<TabBar role="user" />` + 底部留白（此前只有 index/center 有 TabBar，用户进入认领/旅程/采摘园后无法切换导航）
- FarmMap 的固定筛选 chips 上移（bottom:155rpx）避开 TabBar，详情抽屉 z-index 提升

### 5. 修复 TabBar 导航 bug（src/components/TabBar.vue）
- **根因**：`switchTab` 用 `activeIndex === index` 拦截重复点击，但非导航页（如 center）activeIndex 失准为 0（=首页 index），导致"从个人中心点首页"被误拦截
- **修复**：改为用 `currentRoute`（当前真实路由）判断是否已在目标页，彻底消除误拦截

## 二、验证结果

| 验证项 | 结果 |
|--------|------|
| H5 构建 | ✅ DONE Build complete |
| mp-weixin 构建 | ✅ DONE Build complete，真实小程序包（28 页面 app.json + wxml） |
| mp 产物无 SVG | ✅ FarmMap.wxml 无 `<svg>`（已降级列表） |
| 浏览器 5 tab 循环切换 | ✅ **9/9 全部通过**（首页↔认领↔旅程↔采摘园↔我的 任意顺序） |

## 三、项目总进度（六阶段全部完成）

| 阶段 | 内容 | 状态 |
|------|------|------|
| P0 | 认领闭环（认领→合约→溯源→日记→积分→兑换） | ✅ |
| P1 | 旅程合并（模板+AI 生成+手动调整） | ✅ |
| P2 | 游客体验（首页重设计+采摘园地图+收获管理） | ✅ |
| P3 | 农户端（入驻/产品/订单/收益/看板） | ✅ |
| P4 | 政府端（驾驶舱/监管/活动/报告） | ✅ |
| P5 | 小程序（TabBar 农旅化+编译支持+兼容修复） | ✅ |

## 四、运行方式

- 后端：`cd WisdomTree_Service && npm run build && node dist/main.js`（8080）
- Admin：`cd WisdomTree_Admin && npm run dev`（5175）
- H5：`cd WisdomTree_Client && npm run build:h5 && npx vite preview --outDir dist/build/h5`（5173）
- 小程序：`cd WisdomTree_Client && npm run build:mp-weixin`（产物 dist/build/mp-weixin，微信开发者工具导入）
- 账号：admin/farmer_demo/gov_demo/testuser（密码 123456，admin 查 DB）

## 五、整体架构

```
C 端游客（H5+小程序）: 认领/旅程/采摘园地图/收获管理/积分兑换
B 端农户（Admin）   : 产品管理/认领订单/收益(85%)/数据看板
G 端政府（Admin）   : 数据驾驶舱/商户监管/活动统筹/区域报告
后端（NestJS）     : 22+ 模块，MySQL，JWT 三角色鉴权
```

---

## 六、P5 复验记录（2026-08-10）

### 复验结果（六阶段集成回归）
- ✅ `npm run build:mp-weixin` 构建通过（含 P2 首页动态区等新改动）
- ✅ 小程序产物 28 页面 app.json 完整；FarmMap 无 SVG（降级列表 ✅）
- ✅ 首页「村里新鲜事」动态区已编译进小程序 wxml
- ✅ H5 回归 7/7：首页/认领/旅程/采摘园/我的/论坛/AI 全部正常
- ✅ TabBar 5 项（首页/认领/旅程/采摘园/我的）导航正确，无 JS 错误
