# P0 主线开发完成记录（2026-08-08）

> 目标：认领 → 自动合约 → 生长日记 → 积分 → 兑换 完整业务闭环
> 状态：✅ 全部完成并通过验收（15/15）

---

## 一、本轮决策（用户确认）

| 决策 | 选择 |
|------|------|
| 认领对象模型 | A：改 adoptions 实体，tree → product 关联 |
| 认领支付流程 | B：认领直接生效（不经过真实支付） |
| 积分兑换 | 积分直兑（复用 createOrder 的 points_required 抵扣） |
| 前端入口 | H5（Client 改造） |

## 二、后端改动（WisdomTree_Service）

### 实体改造
- `adoptions.entity.ts`：移除 tree/type_id 关联 → 新增 `product`（product_id）、`contract`（contract_id）、`trace_code`、`status`（默认 growing）；nickname/area/wish 改 nullable
- `products.entity.ts`：新增 `adoptable`（默认 true，兼容 2890 条数据）+ `adopt_price`
- `growth_diary.entity.ts`：新增 `user`（user_id）+ `adoption`（adoption_id）关联
- `points.entity.ts`：唯一约束 `(user,date)` → `(user,date,source)`，支持同天多来源积分

### 新增模块 `src/modules/adoption/`
| 接口 | 说明 |
|------|------|
| `POST /adoption` | 认领下单（body: categoryId 或 productId + wish/area/nickname 可选） |
| `GET /adoption/my` | 我的认领列表（产品名/图片/状态/合约/溯源码/日记数） |
| `GET /adoption/:id` | 认领详情 |
| `POST /adoption/:id/diary` | 写日记（+1 积分） |
| `GET /adoption/:id/diary` | 日记列表 |

### 认领联动（事务内 4 件事）
1. 创建认领记录（adoption_id = AD+时间戳+随机4位）
2. 自动生成合约（复用「认养协议」模板 code='adoption'，draft 状态）
3. 生成溯源码建档（T+日期+6位hex，stage=planting）
4. 积分 +50（source='adoption'）+ 预置初始化日记（stage=seedling）

### 积分规则
- 认领 +50、写日记 +1（PointsService 新增 `addPoints(userId, points, source, date?, manager?)`，支持事务）

### 关键修复：categoryId vs productId 契约
- 前端产品列表展示的是**分类**（product_categories），后端认领原来只按**产品**（products）查
- 修复：DTO 支持 `categoryId`（前端传分类 id，后端自动选该分类下第一个未售出产品）或 `productId`
- 实测：分类 12（东北大米）正确认领到东北大米，不再错认到草莓

## 三、前端改动（WisdomTree_Client）

### 新增 5 个页面（pages/adoption/）
| 页面 | 说明 |
|------|------|
| `AdoptionList` | 认领产品列表（产品网格 + 认领按钮 + 分页） |
| `AdoptionForm` | 认领表单（昵称默认取用户 + 区域选择 + 心愿） |
| `MyAdoption` | 我的认领（列表 + 溯源码/认领编号/日记数） |
| `AdoptionDetail` | 认领详情（认领信息 + 溯源 + 日记时间线 + 写日记入口） |
| `AdoptionDiary` | 写日记（阶段下拉 + 描述 + 温度/湿度） |

### 公共工具 `src/common/adoption.js`
- `adoptionRequest`：token 校验 → 401 刷新重试 → code===0 判定
- `normalizeAdoption` / `normalizeDiary`：字段归一化
- `STAGE_OPTIONS` / `STATUS_LABELS` / `AREA_OPTIONS`

### 首页入口
- index.vue 新增「认领农产品」「我的认领」双入口卡片

## 四、环境问题修复

1. **TypeORM 同步失败**：points 表旧唯一索引被外键引用删不掉 → 手动 DROP FOREIGN KEY + DROP INDEX 后重启，TypeORM 自动重建 `(userId,date,source)` 索引
2. **MySQL 容器**：`docker start wisdom-mysql`（root/123456，库 wisdom_tree）
3. **H5 预览**：`npx vite preview --outDir dist/build/h5 --port 5173`

## 五、验收结果（15/15 通过）

认领下单 → 自动生成溯源码/合约/初始化日记 → 我的认领列表 → 认领详情 → 写日记（+1 积分）→ 积分明细 → 积分兑换下单 → 兑换后积分减少（100 积分 = 甜玉米）→ 日记时间线完整

数据库实测：7 条认领、8 份合约、8 条溯源、19 篇日记、积分来源 checkin/adoption/diary 齐全

## 六、服务运行方式

- 后端：`cd WisdomTree_Service && npm run build && node dist/main.js`（端口 8080）
- 前端：`cd WisdomTree_Client && npm run build:h5 && npx vite preview --outDir dist/build/h5`（端口 5173）
- 测试账号：testuser / 123456

## 七、下一步（P1 旅程合并，未开始）

行程+线路合并为「旅程」，AI 生成可改 —— 见 `智慧农旅v2总体方案.md` P1 阶段
