# 采摘园地图真实化改造记录（2026-08-10）

> 目标：把手绘宁夏轮廓升级为真实省界地图（参考文旅部地图风格）
> 状态：✅ 完成并通过验证

---

## 一、改造内容

### 数据源
- **`src/static/map/ningxia.json`**：阿里云 DataV 公开宁夏 GeoJSON（FeatureCollection，5 个地级市 feature，共 2106 个真实省界点，Polygon/MultiPolygon 含飞地/岛屿/洞）

### 后端不动，纯前端改造

| 文件 | 改动 |
|------|------|
| `src/common/farm.js` | 核心：`import ningxiaGeo` 读真实 GeoJSON；`computeGeoBounds` 运行时递归计算真实边界（经度 104.287-107.659 / 纬度 35.238-39.382，**不硬编码**）；`NX_BOUNDS` 基于真实省界；`NX_MAP_VIEW` 调为 100×123（真实比例不变形）；新增 `NX_REGIONS`（五市全部环）、`NX_REGION_FILLS`（北→南 5 档绿 #e8f5e9→#a3d1a8）、`regionPathData`/`regionsPathData`（环→SVG path，evenodd 镂空）；**删除手绘 NX_BORDER** |
| `src/pages/farm/FarmMap/FarmMap.vue` | SVG 渲染 5 市真实轮廓（分市着色 + 白缝 + 深绿描边），标记/城市用同一投影自动对齐 |
| `src/static/css/farm.css` | 画布高度 1485→811rpx（配合 100×123 比例），region/outline 样式 |
| `src/pages/index/index.vue` | 首页小地图顺带升级为真实省界轮廓 |

### 投影实现
纯手写线性投影（无新依赖）：`x=(lng−minLng)/(maxLng−minLng)`、`y=(maxLat−lat)/(maxLat−minLat)`，归一化后乘 viewBox。**省界、城市标注、设施标记共用同一 `projectPoint`**，同源对齐。

## 二、验证结果

| 验证项 | 结果 |
|--------|------|
| H5 构建 | ✅ Build complete，产物含真实省界数据 |
| 省界范围 | ✅ 经度 104.3-107.7 / 纬度 35.2-39.4（真实吻合） |
| 五市位置 | ✅ 石嘴山(北)固原(南)中卫(西) 布局正确 |
| SVG 区域 | ✅ 5 市 + 轮廓，viewBox 100×123 真实比例 |
| 设施标记 | ✅ 12 个，同一投影精准落位 |
| 城市标注 | ✅ 5 市 |
| 详情卡/筛选 | ✅ 功能保留 |

## 三、视觉对比

- **之前**：手绘 27 点简化轮廓，经度范围 105.0-106.6（比真实窄 40%），形状失真
- **现在**：真实省界 2106 点，5 市分色（北浅南深绿），白缝+深绿描边，向文旅部风格靠拢

> 注：当前模型无法直接查看截图，几何正确性已通过数据验证（省界范围、五市投影位置、标记同源对齐）。
