# P1 旅程合并开发完成记录（2026-08-08）

> 目标：把「行程（AI 生成）+ 线路（预置 5 条）」统一为「旅程」概念
> 状态：✅ 全部完成并通过验收（18/18）

---

## 一、设计决策（用户确认）

| 决策 | 选择 |
|------|------|
| 数据模型 | 复用 trip_routes 表，加 user_id（null=模板）/ source（template/ai/manual）/ is_template 字段 |
| AI 行程落库 | 新增接口把 TripTool 生成结果保存为「我的旅程」（source=ai） |
| 手动调整 | H5 用上移/下移/删除按钮，调整后 source 置 manual |
| 前端入口 | 首页新增「智慧旅程」入口卡片 |

## 二、后端改动（WisdomTree_Service）

### 实体
- `trip_routes.entity.ts`：新增 `user_id`（int nullable，null=平台模板）、`source`（varchar 默认 template）、`is_template`（boolean 默认 true）

### 新增接口（全部 JwtAuthGuard，响应 ApiResponse）
| 接口 | 说明 |
|------|------|
| `GET /travel/journeys?type=template\|mine\|all` | 旅程列表（含 stops 驼峰字段） |
| `GET /travel/journeys/:id` | 旅程详情（我的仅本人可见 403，模板任何人） |
| `POST /travel/journeys/save` | 保存旅程（source 传 ai/manual） |
| `POST /travel/journeys/:id/adjust` | 调整站点（事务删旧建新，source 置 manual） |
| `POST /travel/agent/save-journey` | 从 AI schedule 保存（"约2小时"→120min 解析） |

### 关键修复
1. **agent 返回缺 toolResult**：/agent/chat 只返回 reply，前端拿不到结构化行程 → agent.service.ts 返回体加 `toolResult` 字段
2. **TripTool schedule 为空**：8 月无活动时 schedule=0 条 → 用景区 + 采摘产品兜底组装 4 条基础行程
3. **旧接口混入用户旅程**：/travel/routes 用了 `user_id: null`（TypeORM 不认）→ 改 `IsNull()`，恢复只返回 5 条模板

## 三、前端改动（WisdomTree_Client）

### 新增 4 页面（pages/journey/）
| 页面 | 说明 |
|------|------|
| `JourneyList` | 旅程列表（精选模板/我的旅程双 tab） |
| `JourneyDetail` | 详情 + 站点时间线 + 预约/AI 相似/调整入口 |
| `JourneyAI` | AI 生成旅程（问路 + 偏好 chips → 展示结果 → 保存） |
| `JourneyAdjust` | 站点调整（上移/下移/删除 + 保存） |

### 公共工具
- `src/common/journey.js`：journeyRequest（token 刷新 + 401 重试）、normalizeJourney/normalizeStop、extractAIResult、scheduleToStops、parseDurationMinutes、moveStop/removeStop、formatHours/formatDistance

### 首页入口
- index.vue 新增「智慧旅程」入口（金色图标）

### 显示修复
- formatHours/formatDistance 对 0 值返回空串（AI 旅程无时长/里程时不显示"0分钟/0米"）

## 四、验收结果（18/18 通过）

模板旅程列表(5条含站点) → 模板详情(驼峰站点) → AI 返回 toolResult+schedule(≥3条) → AI 旅程保存 → 我的旅程含新AI旅程 → 手动调整反转顺序 → source 置 manual → 模板不可调整(403) → 模板旅程预约 → 我的预约列表 → 旧接口 routes 仍可用(5条)

## 五、运行方式

- 后端：`cd WisdomTree_Service && npm run build && node dist/main.js`（端口 8080）
- 前端：`cd WisdomTree_Client && npm run build:h5 && npx vite preview --outDir dist/build/h5`（端口 5173）
- 测试账号：testuser / 123456
- 数据库：docker 容器 wisdom-mysql（root/123456，库 wisdom_tree）

## 六、下一步（P2 游客体验，未开始）

首页重设计（标语+地图+季节）、采摘园地图、收获管理 —— 见 `智慧农旅v2总体方案.md` P2 阶段
